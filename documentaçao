# Unir do seu branch para a main
    git checkout main
    git merge nome-da-branch

git merge --abort
git merge --continue

git rebase

Tags leves e anotadas

Leves = Commits
Anotadas = Detalhada, metadados, datas e mensagens

# Tag leve

git tag nome-da-tag
git tag 
git tag -d nome-da-tag

# Tag anotada
git tag -s nome-da-tag -m "Descricao da tag"

# Subir uma tag no repositorio
git push origin nome-da-tag

# Remover uma tag do repositório
git push origin --delete nome-da-tag